# Contributing

Thanks for helping improve the project.

## Development setup

```bash
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements-dev.txt
```

Run tests:

```bash
pytest
```

Keep contributions focused, readable, documented, and suitable for authorized
network-security learning.
